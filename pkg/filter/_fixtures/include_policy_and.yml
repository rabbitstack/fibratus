- group: network events
  enabled: true
  policy: include
  relation: and
  from-strings:
    - name: match https connections
      def:  kevt.name = 'Recv' and net.dport = 443
    - name: accept events where source port = 43123
      def:  kevt.name = 'Recv' and net.sport = 43123
