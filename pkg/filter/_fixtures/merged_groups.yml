- group: network events 1
  enabled: true
  policy: include
  relation: or
  from-strings:
    - name: match https connections
      def: kevt.name = 'Recv' and net.dport = 443

- group: network events 2
  enabled: true
  policy: include
  relation: or
  from-strings:
    - name: match http connections
      def: kevt.category = 'net' and net.dport = 80

- group: network events 3
  enabled: true
  policy: include
  relation: or
  from-strings:
    - name: match ssh connections
      def: kevt.name = 'Recv' and net.dport = 22
