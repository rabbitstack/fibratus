- group: network events
  enabled: true
  policy: exclude
  relation: and
  from-strings:
    - name: ignore https connections
      def:  kevt.name = 'Recv' and net.dport = 80
    - name: ignore events where source port != 44123
      def:  kevt.name = 'Recv' and net.sport != 44123
